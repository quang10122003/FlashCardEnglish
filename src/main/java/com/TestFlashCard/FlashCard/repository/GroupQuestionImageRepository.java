package com.TestFlashCard.FlashCard.repository;

import com.TestFlashCard.FlashCard.entity.GroupQuestionImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroupQuestionImageRepository extends JpaRepository<GroupQuestionImage, Integer> {
}
